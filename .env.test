# Modo de tests (vite --mode test): fuerza el demo mode 100% en memoria.
# Gana sobre las credenciales de .env.local por el || en src/config/env.ts.
# Sin secretos: seguro de commitear (lo necesita CI para los e2e).
VITE_DEMO_MODE=true
